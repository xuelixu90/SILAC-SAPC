% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render_notebook.R
\name{render_analysis_notebook}
\alias{render_analysis_notebook}
\title{Render the execution notebook (HTML + Markdown)}
\usage{
render_analysis_notebook(
  input_file,
  output_dir,
  sample_info = build_sample_info(),
  params = serum_source_params()
)
}
\arguments{
\item{input_file}{workbook path used by the run}

\item{output_dir}{destination directory for the generated files}

\item{sample_info}{sample mapping table}

\item{params}{a [serum_source_params()] object}
}
\value{
a named list with two paths: `html` and `markdown`
}
\description{
Runs the bundled `analysis_notebook.Rmd` against the real parameters of
the current analysis so the resulting files contain the full log: code,
console messages, intermediate tables, and every plot.
}
\details{
The Markdown version is rendered so users can inspect the run without a
browser; the HTML version is a full self-contained notebook that can be
opened directly.
}
